<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <style name="LoginTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="base_clouds" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@drawable/clouds</item>
    </style>

    <style name="cloud_button" parent="AppTheme">
        <item name="android:background">@drawable/cloud_3</item>
        <item name="android:color">@android:color/transparent</item>
        <item name="android:scaleType">fitXY</item>
    </style>

    <style name="cloud_view" parent="AppTheme">
        <item name="android:background">@color/cloudy_white</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="MyDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@color/cloudy_white</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
